/**
 * TTS配置
 *
 * 文本转语音相关配置
 */

/**
 * TTS配置
 */
export const TTS_CONFIG = {
  /**
   * 流式TTS文本长度阈值（字符数）
   * 超过此长度使用流式TTS，否则使用普通TTS
   * 
   * 默认值：100字符
   * 可以根据实际需求调整：
   * - 较小值（如50）：更多文本使用流式TTS，减少等待时间
   * - 较大值（如500）：更多文本使用普通TTS，减少服务器压力
   */
  STREAM_THRESHOLD: 100,

  /**
   * 分段播放的段落长度（字符数）
   * 长文本会按此长度分段，逐段转换和播放
   * 
   * 默认值：200字符
   * 可以根据实际需求调整：
   * - 较小值（如100）：更快的首段播放，但需要更多API调用
   * - 较大值（如500）：减少API调用，但首段等待时间更长
   */
  SEGMENT_LENGTH: 200,
} as const;

