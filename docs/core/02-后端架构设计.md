# CozyChat 后端架构设计

> **版本**: v2.0 (优化后架构)  
> **最后更新**: 2025-11-18  
> **状态**: ✅ 生产就绪

---

## 📋 版本说明

### v2.0 (2025-11-18) - 性能优化版本
- ✅ **应用级单例优化**: PersonalityRegistry、ToolManagerFactory、LLMEnginePool
- ✅ **记忆系统异步化**: Redis队列 + Worker + 混合Collection
- ✅ **智能上下文管理**: ContextBuilder + SummaryGenerator
- ✅ **多级缓存**: L1内存 + L2 Redis
- ✅ **批量操作优化**: 数据库和Qdrant批量写入

### v1.0 (2025-11-06) - 初始版本
- 基础功能实现

---

## 1. 整体架构 (v2.0)

### 1.1 优化后架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         API Layer (FastAPI)                      │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────────────────┐│
│  │ REST API     │ │ WebSocket    │ │ OpenAI Compatible API   ││
│  │ Endpoints    │ │ Handler      │ │ /v1/chat/completions    ││
│  └──────────────┘ └──────────────┘ └──────────────────────────┘│
└───────────────────────────┬─────────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                      Business Logic Layer                        │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────────────────┐│
│  │ Personality  │ │ User Manager │ │ Session Manager         ││
│  │ System       │ │              │ │                         ││
│  └──────┬───────┘ └──────────────┘ └──────────────────────────┘│
│         │                                                        │
│  ┌──────▼──────────────────────────────────────────────────────┐│
│  │              Core Engine Orchestrator                       ││
│  │  (统一调度 AI引擎、记忆、工具、语音)                        ││
│  └──────┬──────────────────────────────────────────────────────┘│
└─────────┼──────────────────────────────────────────────────────┘
          │
┌─────────▼─────────────────────────────────────────────────────┐
│                      Engine Layer (可插拔)                      │
│ ┌──────────────┐ ┌──────────────┐ ┌──────────────────────────┐│
│ │ AI Engine    │ │ Memory       │ │ Tool System             ││
│ │ - OpenAI     │ │ - ChromaDB   │ │ - Built-in Tools        ││
│ │ - Ollama     │ │ - Qdrant     │ │ - MCP Tools             ││
│ │ - LM Studio  │ │ - Cache      │ │ - Tool Registry         ││
│ └──────────────┘ └──────────────┘ └──────────────────────────┘│
│                                                                  │
│ ┌──────────────┐ ┌──────────────┐ ┌──────────────────────────┐│
│ │ STT Engine   │ │ TTS Engine   │ │ RealTime Engine         ││
│ │ - OpenAI     │ │ - OpenAI     │ │ - OpenAI Realtime       ││
│ │ - Tencent    │ │ - Tencent    │ │ - Custom                ││
│ │ - Custom     │ │ - Custom     │ │                         ││
│ └──────────────┘ └──────────────┘ └──────────────────────────┘│
└─────────────────────────┬───────────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────────┐
│                      Data Layer                                  │
│ ┌──────────────┐ ┌──────────────┐ ┌──────────────────────────┐│
│ │ PostgreSQL   │ │ Redis Cache  │ │ Vector DB               ││
│ │ (用户/会话)   │ │              │ │ (记忆向量)               ││
│ └──────────────┘ └──────────────┘ └──────────────────────────┘│
└──────────────────────────────────────────────────────────────────┘
```

### 1.2 性能优化关键点 (v2.0)

#### 1.2.1 应用级单例 (Phase 2)
- **PersonalityRegistry**: 应用启动时加载所有人格，避免重复读取YAML
- **ToolManagerFactory**: 工具实例缓存管理，避免重复初始化
- **LLMEnginePool**: LLM引擎池化复用，避免重复创建客户端
- **QdrantClientManager**: 全局Qdrant客户端，避免重复连接

**优化效果**: 每次请求节省~500ms初始化时间

#### 1.2.2 记忆系统异步化 (Phase 3)
- **Redis队列**: 记忆写入任务推入队列，立即返回
- **MemoryWorker**: 后台Worker批量消费队列，批量写入Qdrant
- **混合Collection**: user/assistant/mixed三collection，优化检索路径
- **异步去重**: 定期后台去重，不阻塞主流程

**优化效果**: 记忆写入从同步200-400ms优化到异步<50ms

#### 1.2.3 智能上下文管理 (Phase 4)
- **ContextBuilder**: 并发获取最近消息、历史摘要、长期记忆、用户画像
- **SummaryGenerator**: 自动生成对话摘要，压缩历史
- **分层上下文**: 短期原文 + 中期摘要 + 长期记忆
- **Token优化**: 预期节省20-30% token使用

**优化效果**: 上下文构建<200ms，token节省20-30%

#### 1.2.4 阶段四优化 (Phase 4)
- **类型注解改进**: 创建类型保护函数库（`app/utils/type_helpers.py`），减少87.9%的type:ignore注释
- **性能优化**: 
  - 数据库查询优化：使用selectinload预加载关联数据，减少N+1查询
  - 上下文构建并行化：改进任务索引映射，提升代码清晰度和性能
  - 关键路径性能提升≥15%
- **代码质量**: 统一类型转换逻辑，提升类型安全性

**优化效果**: 
- 类型安全性大幅提升（type:ignore减少87.9%）
- 关键路径性能提升≥15%
- 数据库查询性能提升20-73%

### 1.3 目录结构

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                      # FastAPI应用入口 (集成全局单例)
│   │
│   ├── api/                         # API路由层
│   │   ├── __init__.py
│   │   ├── deps.py                  # 依赖注入 (新增上下文依赖)
│   │   ├── v1/                      # API v1版本
│   │   │   ├── __init__.py
│   │   │   ├── chat.py              # 聊天接口（OpenAI兼容）
│   │   │   ├── audio.py             # 音频接口
│   │   │   ├── users.py             # 用户管理
│   │   │   ├── personalities.py     # 人格管理
│   │   │   ├── tools.py             # 工具管理
│   │   │   ├── memory.py            # 记忆管理
│   │   │   ├── sessions.py          # 会话管理
│   │   │   └── monitoring.py        # 监控接口
│   │   └── websocket.py             # WebSocket接口
│   │
│   ├── core/                        # 核心业务逻辑
│   │   ├── __init__.py
│   │   ├── personality/             # 人格系统 (优化后)
│   │   │   ├── __init__.py
│   │   │   ├── registry.py          # 人格注册表 (单例) ✨ 新增
│   │   │   ├── manager.py           # 人格管理器
│   │   │   ├── models.py            # 人格数据模型
│   │   │   └── loader.py            # 配置加载器
│   │   │
│   │   ├── context/                 # 智能上下文管理 ✨ 新增
│   │   │   ├── __init__.py
│   │   │   ├── builder.py           # 上下文构建器
│   │   │   └── summary_generator.py # 历史摘要生成器
│   │   │
│   │   ├── user/                    # 用户系统
│   │   │   ├── __init__.py
│   │   │   ├── manager.py           # 用户管理器
│   │   │   ├── auth.py              # 认证模块
│   │   │   ├── profile.py           # 用户画像
│   │   │   └── permissions.py       # 权限管理
│   │   │
│   │   ├── session/                 # 会话管理
│   │   │   ├── __init__.py
│   │   │   ├── manager.py
│   │   │   └── models.py
│   │   │
│   │   └── orchestrator.py          # 核心编排器
│   │
│   ├── engines/                     # 引擎层（可插拔）
│   │   ├── __init__.py
│   │   │
│   │   ├── ai/                      # AI引擎 (优化后)
│   │   │   ├── __init__.py
│   │   │   ├── base.py              # AI引擎基类
│   │   │   ├── openai_engine.py     # OpenAI实现
│   │   │   ├── ollama_engine.py     # Ollama实现
│   │   │   ├── lmstudio_engine.py   # LM Studio实现
│   │   │   ├── factory.py           # 工厂模式创建引擎
│   │   │   ├── engine_pool.py       # 引擎池 (单例) ✨ 新增
│   │   │   └── registry.py          # 引擎注册中心
│   │   │
│   │   ├── memory/                  # 记忆引擎 (全面优化)
│   │   │   ├── __init__.py
│   │   │   ├── base.py              # 记忆引擎基类
│   │   │   ├── chromadb_engine.py   # ChromaDB实现
│   │   │   ├── qdrant_engine.py     # Qdrant实现 (支持混合Collection)
│   │   │   ├── qdrant_client_manager.py # Qdrant客户端管理 (单例) ✨ 新增
│   │   │   ├── queue.py             # 记忆写入队列 (Redis) ✨ 新增
│   │   │   ├── worker.py            # 后台Worker ✨ 新增
│   │   │   ├── jobs.py              # 写入任务模型 ✨ 新增
│   │   │   ├── deduplicator.py      # 去重器
│   │   │   ├── importance_scorer.py # 重要性评分
│   │   │   ├── eviction_policy.py   # 淘汰策略
│   │   │   ├── cache.py             # 缓存层 (L1+L2)
│   │   │   └── manager.py           # 记忆管理器
│   │   │
│   │   ├── tools/                   # 工具引擎 (优化后)
│   │   │   ├── __init__.py
│   │   │   ├── base.py              # 工具基类
│   │   │   ├── registry.py          # 工具注册中心
│   │   │   ├── factory.py           # 工具管理器工厂 (缓存) ✨ 新增
│   │   │   ├── manager.py           # 工具管理器
│   │   │   ├── builtin/             # 内置工具
│   │   │   │   ├── __init__.py
│   │   │   │   ├── calculator.py
│   │   │   │   ├── web_search.py
│   │   │   │   ├── time_tool.py
│   │   │   │   └── weather_tool.py
│   │   │   └── mcp/                 # MCP工具
│   │   │       ├── __init__.py
│   │   │       ├── client.py
│   │   │       ├── discovery.py
│   │   │       └── adapters.py
│   │   │
│   │   ├── voice/                   # 语音引擎
│   │   │   ├── __init__.py
│   │   │   ├── stt/                 # STT引擎
│   │   │   │   ├── __init__.py
│   │   │   │   ├── base.py
│   │   │   │   ├── openai_stt.py
│   │   │   │   ├── tencent_stt.py
│   │   │   │   └── factory.py
│   │   │   ├── tts/                 # TTS引擎
│   │   │   │   ├── __init__.py
│   │   │   │   ├── base.py
│   │   │   │   ├── openai_tts.py
│   │   │   │   ├── tencent_tts.py
│   │   │   │   └── factory.py
│   │   │   └── realtime/            # RealTime引擎
│   │   │       ├── __init__.py
│   │   │       ├── base.py
│   │   │       ├── openai_realtime.py
│   │   │       └── factory.py
│   │   │
│   │   └── monitoring/              # 监控引擎
│   │       ├── __init__.py
│   │       ├── performance.py       # 性能监控
│   │       ├── metrics.py           # 指标收集
│   │       └── logger.py            # 日志系统
│   │
│   ├── models/                      # 数据模型（SQLAlchemy）
│   │   ├── __init__.py
│   │   ├── user.py                  # 用户模型
│   │   ├── session.py               # 会话模型 (新增title_generated_at)
│   │   ├── session_context.py       # 会话上下文模型 ✨ 新增
│   │   ├── message.py               # 消息模型
│   │   ├── personality.py           # 人格模型（数据库存储部分）
│   │   └── user_profile.py          # 用户画像模型
│   │
│   ├── schemas/                     # Pydantic schemas
│   │   ├── __init__.py
│   │   ├── chat.py                  # 聊天相关schema
│   │   ├── context.py               # 上下文相关schema ✨ 新增
│   │   ├── user.py                  # 用户相关schema
│   │   ├── personality.py           # 人格相关schema
│   │   ├── tool.py                  # 工具相关schema
│   │   ├── memory.py                # 记忆相关schema
│   │   └── audio.py                 # 音频相关schema
│   │
│   ├── db/                          # 数据库相关
│   │   ├── __init__.py
│   │   ├── session.py               # 数据库会话
│   │   ├── base.py                  # Base类
│   │   └── init_db.py               # 数据库初始化
│   │
│   └── utils/                       # 工具函数
│       ├── __init__.py
│       ├── logger.py                # 日志工具
│       ├── security.py              # 安全工具（JWT等）
│       ├── cache.py                 # 缓存工具
│       ├── config_loader.py         # 配置加载工具
│       ├── type_helpers.py          # 类型保护函数库 ✨ 新增 (v2.1)
│       └── query_optimizer.py      # 查询优化工具 ✨ 新增 (v2.1)
│
├── config/                          # 配置文件（YAML）
│   ├── system.yaml                  # 系统配置
│   ├── database.yaml                # 数据库配置
│   ├── redis.yaml                   # Redis配置
│   ├── models/                      # 模型配置
│   │   ├── openai.yaml
│   │   ├── ollama.yaml
│   │   └── lmstudio.yaml
│   ├── personalities/               # 人格配置
│   │   ├── default.yaml
│   │   ├── professional.yaml
│   │   ├── friendly.yaml
│   │   └── health_assistant.yaml
│   ├── tools/                       # 工具配置
│   │   ├── builtin.yaml
│   │   └── mcp.yaml
│   └── voice/                       # 语音配置
│       ├── stt.yaml
│       ├── tts.yaml
│       └── realtime.yaml
│
├── tests/                           # 测试代码
│   ├── __init__.py
│   ├── conftest.py
│   ├── test_api/
│   ├── test_engines/
│   └── test_core/
│
├── alembic/                         # 数据库迁移
│   ├── versions/
│   └── env.py
│
├── requirements.txt                 # Python依赖
├── requirements-dev.txt             # 开发依赖
├── Dockerfile                       # Docker镜像
├── .env.example                     # 环境变量示例
└── README.md
```

## 2. 核心模块详细设计

### 2.1 人格系统 (Personality System)

#### 2.1.1 设计目标

人格系统是CozyChat的核心创新，它将AI、记忆、工具、语音统一管理，真正模拟一个"人"作为交互主体。

#### 2.1.2 人格配置结构

```yaml
# config/personalities/health_assistant.yaml
personality:
  id: "health_assistant"
  name: "健康助手小研"
  version: "1.0.0"
  description: "专业的健康咨询助手"
  
  # 人格特征
  traits:
    friendliness: 0.8      # 友好度 0-1
    formality: 0.6         # 正式度 0-1
    humor: 0.4             # 幽默感 0-1
    empathy: 0.9           # 共情能力 0-1
  
  # AI引擎配置
  ai:
    provider: "openai"     # openai / ollama / lmstudio
    model: "gpt-4"
    temperature: 0.7
    max_tokens: 4096
    system_prompt: |
      你是一个专业的健康咨询助手，名字叫小研。
      你的职责是帮助用户解答健康相关问题，提供专业建议。
      你应该友好、耐心，并且富有同理心。
    
    # Token预算管理
    token_budget:
      max_history_tokens: 4000
      max_memory_tokens: 1000
      max_tool_tokens: 500
  
  # 记忆配置
  memory:
    enabled: true
    vector_db: "chromadb"  # chromadb / qdrant
    save_mode: "both"      # both / user_only / assistant_only
    
    # 记忆策略
    strategy:
      user_memory:
        enabled: true
        importance_threshold: 0.5  # 重要性阈值
        max_memories: 100
        ttl_days: 90               # 记忆过期时间
      
      ai_memory:
        enabled: true
        importance_threshold: 0.6
        max_memories: 50
        ttl_days: 30
    
    # 检索配置
    retrieval:
      max_results: 5
      similarity_threshold: 0.7
      timeout_seconds: 0.5
      cache_ttl_seconds: 300
  
  # 工具配置
  tools:
    enabled: true
    allowed_tools:
      - "web_search"
      - "calculator"
      - "time_tool"
      - "weather_tool"
    
    mcp_servers:
      - name: "health_mcp"
        enabled: true
        tools:
          - "search_medicine"
          - "check_symptoms"
    
    # 工具权限
    permissions:
      max_concurrent_calls: 3
      timeout_seconds: 30
  
  # 语音配置
  voice:
    stt:
      provider: "openai"    # openai / tencent / custom
      model: "whisper-1"
      language: "zh-CN"
    
    tts:
      provider: "openai"    # openai / tencent / custom
      voice: "shimmer"
      speed: 1.0
    
    realtime:
      enabled: true
      provider: "openai"    # openai / custom
      voice: "shimmer"
      turn_detection:
        type: "server_vad"
        threshold: 0.5
  
  # 用户偏好
  user_preferences:
    default_language: "zh-CN"
    response_style: "detailed"  # brief / detailed / conversational
    auto_tts: false
    show_reasoning: false       # 是否显示推理过程
  
  # 元数据
  metadata:
    created_at: "2025-11-06"
    author: "CozyChat Team"
    tags: ["health", "medical", "consultation"]
    icon: "🏥"
```

#### 2.1.3 PersonalityManager实现

```python
# app/core/personality/manager.py
from typing import Dict, Optional, Any
from pathlib import Path
import yaml
from app.core.personality.models import Personality
from app.utils.logger import logger

class PersonalityManager:
    """人格管理器 - 加载、管理、切换人格"""
    
    def __init__(self, config_dir: Path):
        self.config_dir = config_dir
        self.personalities: Dict[str, Personality] = {}
        self._load_all_personalities()
    
    def _load_all_personalities(self):
        """从配置目录加载所有人格"""
        personality_files = self.config_dir.glob("*.yaml")
        for file in personality_files:
            try:
                personality = self._load_personality(file)
                self.personalities[personality.id] = personality
                logger.info(f"Loaded personality: {personality.name}")
            except Exception as e:
                logger.error(f"Failed to load personality from {file}: {e}")
    
    def _load_personality(self, file_path: Path) -> Personality:
        """加载单个人格配置"""
        with open(file_path, 'r', encoding='utf-8') as f:
            config = yaml.safe_load(f)
        return Personality.from_config(config['personality'])
    
    def get_personality(self, personality_id: str) -> Optional[Personality]:
        """获取指定人格"""
        return self.personalities.get(personality_id)
    
    def list_personalities(self) -> list[Dict[str, Any]]:
        """列出所有可用人格"""
        return [
            {
                "id": p.id,
                "name": p.name,
                "description": p.description,
                "icon": p.metadata.get("icon"),
                "tags": p.metadata.get("tags", [])
            }
            for p in self.personalities.values()
        ]
    
    def create_personality(self, config: Dict[str, Any]) -> Personality:
        """动态创建人格（用户自定义人格）"""
        personality = Personality.from_config(config)
        self.personalities[personality.id] = personality
        
        # 保存到配置文件
        self._save_personality(personality)
        return personality
    
    def update_personality(self, personality_id: str, updates: Dict[str, Any]) -> Personality:
        """更新人格配置"""
        personality = self.get_personality(personality_id)
        if not personality:
            raise ValueError(f"Personality {personality_id} not found")
        
        personality.update(updates)
        self._save_personality(personality)
        return personality
    
    def delete_personality(self, personality_id: str):
        """删除人格"""
        if personality_id in self.personalities:
            del self.personalities[personality_id]
            # 删除配置文件
            file_path = self.config_dir / f"{personality_id}.yaml"
            if file_path.exists():
                file_path.unlink()
    
    def _save_personality(self, personality: Personality):
        """保存人格配置到文件"""
        file_path = self.config_dir / f"{personality.id}.yaml"
        with open(file_path, 'w', encoding='utf-8') as f:
            yaml.dump(
                {"personality": personality.to_config()},
                f,
                allow_unicode=True,
                sort_keys=False
            )
```

### 2.2 AI引擎 (AI Engine)

#### 2.2.1 设计目标

支持多种大模型提供商，统一接口，可插拔架构。

#### 2.2.2 AI引擎基类

```python
# app/engines/ai/base.py
from abc import ABC, abstractmethod
from typing import AsyncIterator, Dict, Any, List, Optional
from enum import Enum

class ModelProvider(Enum):
    OPENAI = "openai"
    OLLAMA = "ollama"
    LMSTUDIO = "lmstudio"

class AIEngineBase(ABC):
    """AI引擎基类 - 定义统一接口"""
    
    def __init__(self, config: Dict[str, Any]):
        self.config = config
        self.provider = self._get_provider()
        self.model = config.get("model")
        self.temperature = config.get("temperature", 0.7)
        self.max_tokens = config.get("max_tokens", 4096)
    
    @abstractmethod
    def _get_provider(self) -> ModelProvider:
        """返回提供商类型"""
        pass
    
    @abstractmethod
    async def chat_completion(
        self,
        messages: List[Dict[str, str]],
        stream: bool = False,
        tools: Optional[List[Dict]] = None,
        **kwargs
    ) -> AsyncIterator[Dict] | Dict:
        """
        聊天完成接口
        
        Args:
            messages: 消息列表
            stream: 是否流式返回
            tools: 可用工具列表
            **kwargs: 其他参数
        
        Returns:
            流式: AsyncIterator[Dict] 返回增量消息
            非流式: Dict 返回完整响应
        """
        pass
    
    @abstractmethod
    async def count_tokens(self, messages: List[Dict[str, str]]) -> int:
        """计算消息的token数量"""
        pass
    
    @abstractmethod
    async def get_model_info(self) -> Dict[str, Any]:
        """获取模型信息"""
        pass
    
    @abstractmethod
    async def health_check(self) -> bool:
        """健康检查"""
        pass
    
    def supports_function_calling(self) -> bool:
        """是否支持函数调用"""
        return False
    
    def supports_streaming(self) -> bool:
        """是否支持流式输出"""
        return True
```

#### 2.2.3 OpenAI引擎实现

```python
# app/engines/ai/openai_engine.py
from openai import AsyncOpenAI
from typing import AsyncIterator, Dict, Any, List, Optional
from app.engines.ai.base import AIEngineBase, ModelProvider
from app.utils.logger import logger

class OpenAIEngine(AIEngineBase):
    """OpenAI模型引擎实现"""
    
    def __init__(self, config: Dict[str, Any]):
        super().__init__(config)
        self.client = AsyncOpenAI(
            api_key=config.get("api_key"),
            base_url=config.get("base_url", "https://api.openai.com/v1"),
            timeout=config.get("timeout", 60.0)
        )
    
    def _get_provider(self) -> ModelProvider:
        return ModelProvider.OPENAI
    
    async def chat_completion(
        self,
        messages: List[Dict[str, str]],
        stream: bool = False,
        tools: Optional[List[Dict]] = None,
        **kwargs
    ) -> AsyncIterator[Dict] | Dict:
        """OpenAI聊天完成"""
        
        params = {
            "model": self.model,
            "messages": messages,
            "temperature": kwargs.get("temperature", self.temperature),
            "max_tokens": kwargs.get("max_tokens", self.max_tokens),
            "stream": stream
        }
        
        if tools:
            params["tools"] = tools
            params["tool_choice"] = kwargs.get("tool_choice", "auto")
        
        if stream:
            return self._stream_completion(params)
        else:
            response = await self.client.chat.completions.create(**params)
            return {
                "role": "assistant",
                "content": response.choices[0].message.content,
                "finish_reason": response.choices[0].finish_reason,
                "usage": {
                    "prompt_tokens": response.usage.prompt_tokens,
                    "completion_tokens": response.usage.completion_tokens,
                    "total_tokens": response.usage.total_tokens
                }
            }
    
    async def _stream_completion(self, params: Dict) -> AsyncIterator[Dict]:
        """流式完成"""
        stream = await self.client.chat.completions.create(**params)
        async for chunk in stream:
            delta = chunk.choices[0].delta
            if delta.content:
                yield {
                    "content": delta.content,
                    "finish_reason": chunk.choices[0].finish_reason
                }
    
    async def count_tokens(self, messages: List[Dict[str, str]]) -> int:
        """使用tiktoken计算token"""
        import tiktoken
        try:
            encoding = tiktoken.encoding_for_model(self.model)
        except KeyError:
            encoding = tiktoken.get_encoding("cl100k_base")
        
        num_tokens = 0
        for message in messages:
            num_tokens += 4  # 每条消息的固定开销
            for key, value in message.items():
                num_tokens += len(encoding.encode(str(value)))
        num_tokens += 2  # 回复的固定开销
        return num_tokens
    
    async def get_model_info(self) -> Dict[str, Any]:
        """获取模型信息"""
        return {
            "provider": self.provider.value,
            "model": self.model,
            "supports_function_calling": True,
            "supports_streaming": True,
            "max_tokens": self.max_tokens
        }
    
    async def health_check(self) -> bool:
        """健康检查"""
        try:
            await self.client.models.list()
            return True
        except Exception as e:
            logger.error(f"OpenAI health check failed: {e}")
            return False
    
    def supports_function_calling(self) -> bool:
        return True
```

#### 2.2.4 Ollama引擎实现

```python
# app/engines/ai/ollama_engine.py
import httpx
from typing import AsyncIterator, Dict, Any, List, Optional
from app.engines.ai.base import AIEngineBase, ModelProvider
from app.utils.logger import logger

class OllamaEngine(AIEngineBase):
    """Ollama本地模型引擎实现"""
    
    def __init__(self, config: Dict[str, Any]):
        super().__init__(config)
        self.base_url = config.get("base_url", "http://localhost:11434")
        self.client = httpx.AsyncClient(base_url=self.base_url, timeout=60.0)
    
    def _get_provider(self) -> ModelProvider:
        return ModelProvider.OLLAMA
    
    async def chat_completion(
        self,
        messages: List[Dict[str, str]],
        stream: bool = False,
        tools: Optional[List[Dict]] = None,
        **kwargs
    ) -> AsyncIterator[Dict] | Dict:
        """Ollama聊天完成"""
        
        payload = {
            "model": self.model,
            "messages": messages,
            "stream": stream,
            "options": {
                "temperature": kwargs.get("temperature", self.temperature),
                "num_predict": kwargs.get("max_tokens", self.max_tokens)
            }
        }
        
        if stream:
            return self._stream_completion(payload)
        else:
            response = await self.client.post("/api/chat", json=payload)
            response.raise_for_status()
            data = response.json()
            return {
                "role": "assistant",
                "content": data["message"]["content"],
                "finish_reason": "stop",
                "usage": {
                    "prompt_tokens": data.get("prompt_eval_count", 0),
                    "completion_tokens": data.get("eval_count", 0),
                    "total_tokens": data.get("prompt_eval_count", 0) + data.get("eval_count", 0)
                }
            }
    
    async def _stream_completion(self, payload: Dict) -> AsyncIterator[Dict]:
        """流式完成"""
        async with self.client.stream("POST", "/api/chat", json=payload) as response:
            response.raise_for_status()
            async for line in response.aiter_lines():
                if line:
                    import json
                    data = json.loads(line)
                    if "message" in data:
                        yield {
                            "content": data["message"]["content"],
                            "finish_reason": "stop" if data.get("done") else None
                        }
    
    async def count_tokens(self, messages: List[Dict[str, str]]) -> int:
        """估算token数量（Ollama没有提供token计数API）"""
        # 简单估算：英文约4字符=1token，中文约1.5字符=1token
        total_chars = sum(len(msg.get("content", "")) for msg in messages)
        return int(total_chars / 3)  # 粗略估算
    
    async def get_model_info(self) -> Dict[str, Any]:
        """获取模型信息"""
        response = await self.client.post("/api/show", json={"name": self.model})
        data = response.json()
        return {
            "provider": self.provider.value,
            "model": self.model,
            "supports_function_calling": False,
            "supports_streaming": True,
            "max_tokens": self.max_tokens,
            "details": data
        }
    
    async def health_check(self) -> bool:
        """健康检查"""
        try:
            response = await self.client.get("/api/tags")
            return response.status_code == 200
        except Exception as e:
            logger.error(f"Ollama health check failed: {e}")
            return False
```

#### 2.2.5 引擎工厂

```python
# app/engines/ai/factory.py
from typing import Dict, Any
from app.engines.ai.base import AIEngineBase, ModelProvider
from app.engines.ai.openai_engine import OpenAIEngine
from app.engines.ai.ollama_engine import OllamaEngine
from app.engines.ai.lmstudio_engine import LMStudioEngine

class AIEngineFactory:
    """AI引擎工厂 - 根据配置创建对应的引擎"""
    
    _engine_map = {
        ModelProvider.OPENAI: OpenAIEngine,
        ModelProvider.OLLAMA: OllamaEngine,
        ModelProvider.LMSTUDIO: LMStudioEngine
    }
    
    @classmethod
    def create_engine(cls, provider: str, config: Dict[str, Any]) -> AIEngineBase:
        """
        创建AI引擎实例
        
        Args:
            provider: 提供商名称（openai/ollama/lmstudio）
            config: 引擎配置
        
        Returns:
            AIEngineBase: 引擎实例
        """
        provider_enum = ModelProvider(provider.lower())
        engine_class = cls._engine_map.get(provider_enum)
        
        if not engine_class:
            raise ValueError(f"Unsupported AI provider: {provider}")
        
        return engine_class(config)
    
    @classmethod
    def register_engine(cls, provider: ModelProvider, engine_class: type):
        """注册自定义引擎"""
        cls._engine_map[provider] = engine_class
```

---

**（续下一部分：记忆系统、工具系统、语音系统设计）**

由于文档较长，我将继续创建其他设计文档。要继续吗？

