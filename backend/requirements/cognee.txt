# Cognee 依赖（需要先安装 core.txt）
# 安装方式：pip install -r requirements/cognee.txt
# 注意：必须先安装 core.txt: pip install -r requirements/core.txt

# Cognee Python SDK（可选，用于知识图谱记忆存储）
# 注意：
# 1. Cognee SDK 通过 HTTP API 调用 Cognee 服务（需要先部署独立的 Cognee API 服务器）
# 2. 如果未安装，代码会自动回退到 Qdrant（代码中有容错处理）
# 3. 配置方式：在 backend/config/memory.yaml 中设置 default_engine: "cognee" 和 api_url
# 4. 轻量级 SDK（5-10MB），替代完整的 cognee 库（500MB-2GB）
cognee-sdk>=0.1.1

# Cognee SDK 相关依赖（SDK 本身已包含，这里列出用于参考）
# 注意：cognee-sdk 的依赖：
# - httpx>=0.25.0（HTTP 客户端）
# - pydantic>=2.0.0（数据验证）
# - typing-extensions>=4.0.0（类型扩展）
# 
# 可选依赖（WebSocket 支持）：
# - websockets>=12.0（pip install cognee-sdk[websocket]）
#
# 注意：不再需要以下 cognee 库的依赖：
# - aiohttp, yarl, fastapi-users, tokenizers, hf-xet
# - numpy, grpcio 等（这些由 SDK 的依赖管理）

