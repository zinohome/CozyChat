# Web框架
fastapi==0.121.0
uvicorn[standard]==0.38.0
pydantic==2.12.4
pydantic-settings==2.11.0
pydantic[email]==2.12.4
python-multipart==0.0.20

# 数据库
sqlalchemy==2.0.44
alembic==1.17.1
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Redis
redis==7.0.1
hiredis==2.3.2

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2  # 使用3.x版本以兼容passlib 1.7.4（4.x版本移除了__about__属性）

# AI/LLM
openai==2.7.1
httpx==0.28.1
# websockets 版本约束
# 注意：cognee>=0.4.1 要求 websockets>=15.0.1,<16.0.0
# 更新版本以满足 cognee 的要求
websockets>=15.0.1,<16.0.0
# urllib3 版本约束（解决依赖解析深度问题）
# 注意：qdrant-client 需要 urllib3<3,>=1.26.14，锁定版本范围帮助 pip 快速解析
urllib3>=2.0.0,<3.0.0
# aiohttp 版本约束（解决依赖解析深度问题）
# 注意：cognee 需要 aiohttp<4.0.0,>=3.11.14，锁定版本范围帮助 pip 快速解析
aiohttp>=3.11.14,<4.0.0
# yarl 版本约束（aiohttp 的依赖，锁定版本减少回溯）
# 注意：aiohttp 需要 yarl<2.0,>=1.17.0，锁定到最新兼容版本
yarl>=1.22.0,<2.0.0
# fastapi-users 版本约束（cognee 的依赖，锁定版本减少回溯）
# 注意：cognee 需要 fastapi-users[sqlalchemy]<15.0.0,>=14.0.1，锁定到最新兼容版本
fastapi-users[sqlalchemy]>=14.0.2,<15.0.0
# tokenizers 版本约束（transformers 的依赖，锁定版本减少回溯）
# 注意：transformers 需要 tokenizers<=0.23.0,>=0.22.0，锁定到最新兼容版本
tokenizers>=0.22.1,<=0.23.0
# hf-xet 版本约束（huggingface-hub 的依赖，锁定版本减少回溯）
# 注意：huggingface-hub 需要 hf-xet<2.0.0,>=1.1.3，锁定到最新兼容版本
hf-xet>=1.2.0,<2.0.0

# 向量数据库
# chromadb==1.3.4  # 已注释：默认使用 qdrant，ChromaDB 作为备选（代码中有容错处理）
qdrant-client==1.11.3
# Cognee 知识图谱框架（可选，用于知识图谱记忆存储）
# 注意：
# 1. Cognee 需要独立的数据库（PostgreSQL、Neo4j、Redis、MinIO），不能复用项目数据库
# 2. 如果未安装，代码会自动回退到 Qdrant（代码中有容错处理）
# 3. 配置方式：在 backend/config/memory.yaml 中设置 default_engine: "cognee"
cognee>=0.4.1
# NumPy 版本约束
# 注意：
# 1. cognee>=0.4.1 要求 numpy>=1.26.4,<=4.0.0
# 2. qdrant-client 1.11.3 要求 numpy>=1.21
# 3. 更新约束以满足 cognee 的要求（如果不需要 cognee，可以改为 numpy>=1.22.0,<2.0）
numpy>=1.26.4,<4.0.0
# Embedding模型
sentence-transformers==2.3.1

# 缓存
cachetools==5.3.2

# 工具
pyyaml==6.0.1
python-dotenv==1.2.1
# tavily-python 版本约束
# ⚠️ 依赖冲突警告：
# - tavily-python 0.3.0 要求 tiktoken==0.5.1（精确版本）
# - cognee>=0.4.1 要求 tiktoken>=0.8.0,<1.0.0
# 解决方案：尝试使用最新版本的 tavily-python（可能支持更新的 tiktoken）
# 如果仍有冲突，可以考虑：
# 1. 不使用 cognee（代码会自动回退到 Qdrant）
# 2. 不使用 tavily-python（如果不需要搜索工具）
tavily-python>=0.3.0
# tiktoken 版本约束（满足 cognee 的要求）
# 注意：如果 tavily-python 0.3.0 无法安装，需要升级 tavily-python 或移除 tavily-python
tiktoken>=0.8.0,<1.0.0
zhconv==1.4.2  # 繁体中文转简体中文

# 日志
structlog==25.5.0

# API限流
slowapi==0.1.9

# 腾讯语音SDK（通过本地包安装）
# 安装方式：pip install -e packages/tencent-speech-sdk
# 注意：需要先初始化git submodule: git submodule update --init --recursive

# 腾讯语音SDK依赖
websocket-client==0.48  # 必须0.48版本（WebSocket实时识别需要）
requests>=2.28.0        # HTTP请求